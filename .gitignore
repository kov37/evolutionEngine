__pycache__/
*.pyc
.DS_Store

# Regeneratable build/eval output, not source:
evolve/swebench_venv/     # `pip install swebench` in a fresh venv rebuilds this (339M)
logs/build_images/        # swebench harness's own Docker build log spew (cwd-relative)
logs/run_evaluation/      # swebench harness's own Docker build log spew (cwd-relative)

.venv-swebench/
assets/swebench-test.parquet
assets/benchmarks/
state/benchmark/runs/sympy-13878-*.json
